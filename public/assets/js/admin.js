const ban=queryAll('.type-ban'),report=queryAll(".report-saw"),badge=queryAll(".remove-badge"),audit=queryAll(".audit-status"),update=queryAll(".update");ban.forEach(el=>el.addEventListener("click",function(e){makeRequest("/mod/admin/"+el.dataset.type+"/ban",options={body:"id="+el.dataset.id})}));report.forEach(el=>el.addEventListener("click",function(e){makeRequest("/mod/admin/reports/saw",options={body:"id="+el.dataset.id})}));badge.forEach(el=>el.addEventListener("click",function(e){makeRequest("/mod/admin/badge/remove",options={body:"id="+el.dataset.id+"&uid="+el.dataset.uid})}));audit.forEach(el=>el.addEventListener("click",function(e){makeRequest("/mod/admin/audit/status",options={body:"status="+el.dataset.id+"@"+el.dataset.status})}));update.forEach(el=>el.addEventListener("click",function(e){makeRequest("/mod/admin/manual/update",options={body:"type="+el.dataset.type})}))